version: "3.3"
services:
  frontend:
    image: registry.gitlab.com/haapsee/mean-template:development
    build:
      context: ./frontend
      target: development
      dockerfile: Dockerfile
    ports:
      - "4200:4200"
